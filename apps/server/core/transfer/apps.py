"""Configuration de l'application Transfer."""

from django.apps import AppConfig


class TransferConfig(AppConfig):
    """Configuration de l'application Transfer."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "core.transfer"
    verbose_name = "Transfer"
